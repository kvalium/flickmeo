{
  "name": "root",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "dev": "npx lerna run dev --parallel --stream",
    "build": "npx lerna run build --parallel --stream",
    "format": "npx lerna run format --parallel --stream",
    "test": "npx lerna run test --parallel --stream",
    "lint": "npx lerna run lint --parallel --stream"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.10.0",
    "@typescript-eslint/parser": "^5.10.0",
    "eslint": "^8.7.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-react": "^7.28.0",
    "eslint-plugin-react-hooks": "^4.3.0",
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  }
}
